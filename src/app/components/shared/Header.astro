---
import H1 from "./Heading/H1.astro";
import IconGoBack from "./icons/IconGoBack.astro";
const { title, hasBackButton } = Astro.props;
// TODO: hasBackButton is
---


<div class="navbar">
	<div class="flex-none">
	  	{hasBackButton && (
			<buttonbutton class="btn btn-square btn-ghost">
				<IconGoBack />
			</buttonbutton>
		)}
	</div>
	<div class:list={[ 
		"flex-1", 
		{ "ml-3": !hasBackButton }
	]}>
		<H1>{title}</H1>	
	</div>
  </div>